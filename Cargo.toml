# Standalone package (not a member of the sibling BDK workspace).
[workspace]

[package]
name = "bdk_wallet"
homepage = "https://bitcoindevkit.org"
version = "3.1.0-litecoin.0"
repository = "https://github.com/IndigoNakamoto/bdk_wallet"
documentation = "https://docs.rs/bdk_wallet"
description = "A modern, lightweight, descriptor-based wallet library for Litecoin"
keywords = ["litecoin", "wallet", "descriptor", "psbt"]
readme = "README.md"
license = "MIT OR Apache-2.0"
authors = ["Bitcoin Dev Kit Developers"]
edition = "2021"
rust-version = "1.85.0"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }

[dependencies]
# Nested clone uses path deps against the sibling Litecoin BDK crates.
bdk_chain = { path = "../crates/chain", version = "0.23.2", features = ["miniscript", "serde"], default-features = false }
bdk_mweb = { path = "../crates/mweb", version = "0.1.0", optional = true, default-features = false, features = ["std", "zkp"] }
bitcoin = { package = "litecoin", version = "0.32.8-rc.1", features = ["serde", "base64"], default-features = false }
miniscript = { git = "https://github.com/IndigoNakamoto/rust-miniscript", rev = "e3187272617adad69f5b9ece62d25ea28b70fd25", features = ["serde"], default-features = false }
rand_core = { version = "0.6.4" }
serde_json = { version = "1" }
serde = { version = "1", features = ["derive"] }

# Optional dependencies
anyhow = { version = "1", optional = true }
bdk_file_store = { path = "../crates/file_store", version = "0.22.0", optional = true }
bip39 = { version = "2.2.2", optional = true }
tempfile = { version = "3.26.0", optional = true }

[features]
default = ["std"]
std = ["bitcoin/std", "bitcoin/rand-std", "miniscript/std", "bdk_chain/std"]
compiler = ["miniscript/compiler"]
all-keys = ["keys-bip39"]
keys-bip39 = ["bip39"]
rusqlite = ["bdk_chain/rusqlite"]
file_store = ["bdk_file_store"]
test-utils = ["std", "anyhow", "tempfile"]
# MWEB facade: CombinedBalance, prepare_mweb_pegin / build_mweb_send / pegout.
mweb = ["dep:bdk_mweb"]

[dev-dependencies]
anyhow = "1"
assert_matches = "1.5.0"
bdk_electrum = { path = "../crates/electrum", version = "0.23.2" }
bdk_esplora = { path = "../crates/esplora", version = "0.22.1", features = ["async-https", "blocking-https", "tokio"] }
bdk_testenv = { path = "../crates/testenv", features = ["litecoin-daemon"] }
bdk_wallet = { path = ".", features = ["rusqlite", "file_store", "test-utils", "mweb"] }
clap = { version = "4.5.60", features = ["derive", "env"] }
ctrlc = "3.5.2"
rand = "0.8"
tempfile = "3"
tokio = { version = "1.38.1", features = ["rt", "rt-multi-thread", "macros"] }

[[example]]
name = "mnemonic_to_descriptors"
path = "examples/mnemonic_to_descriptors.rs"
required-features = ["all-keys"]

[[example]]
name = "miniscriptc"
path = "examples/compiler.rs"
required-features = ["compiler"]

[[example]]
name = "electrum"

[[example]]
name = "esplora_async"

[[example]]
name = "esplora_blocking"
